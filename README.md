# 项目介绍
一个转发的OneBot反向ws消息吃中间层程序。
一个bot可能会接入多个bot应用，它们可能会对同一个消息产生反应，出现多个响应刷屏。
这个程序，可以在不修改bot应用和onebot客户端的前提下处理onebot的消息。
# 使用方法
在config.yaml中配置好本程序的端口，bot的账号。配置bot应用的反向ws连接和过滤器。然后启动本程序，在onebot客户端使用反向ws连接本程序。
